# 施工项目记账程序

一个基于Python和Tkinter的施工项目记账桌面应用程序，用于管理项目的工作类型和账单记录。

## 功能特性

### 单窗口设计
- 左侧边栏显示项目列表，点击切换
- 右侧内容区显示项目详情
- 无需多次打开新窗口，操作更便捷

### 项目管理
- 创建、编辑、删除施工项目
- 项目状态管理：进行中 / 已结账
- 项目列表支持搜索
- 项目数据自动备份

### 工作类型管理
- 预置常见工作类型（泥瓦工程、水电工程等）
- 自定义工作项目、单价和单位
- 支持"按单价"或"无单价"两种计费方式
- 支持工作类型的添加、删除

### 账单管理
- 记录各工作项目的账单
- 每条记录包含日期（默认当天）
- 支持数学算式计算（如：3×4+5）
- 自动计算金额总计
- 双击记录可编辑
- 支持批量删除

### 图片导出
- 将账单记录导出为图片格式
- 自定义导出设置（颜色、显示选项等）

## 界面预览

```
┌──────────────────────────────────────────────────┐
│  施工项目记账程序                                  │
├────────────┬─────────────────────────────────────┤
│            │                                     │
│ [+ 新建项目]│  XX小区装修  ●进行中  [标记为已结账]  │
│ [搜索...]  │  创建：2024-03-15                   │
│            │  ─────────────────────────────────  │
│ ● XX小区装修│  ( ) 账单管理  ( ) 工作类型          │
│ ● 商场改造 │                                     │
│ ○ 仓库维修 │  合计：￥725.00   [+添加记录] [保存图片]│
│            │  ┌─────────────────────────────────┐│
│            │  │工作内容  公式  单价  金额  日期  备注││
│            │  │砌墙      5    45   225  3月15  加油││
│            │  │贴砖      8    40   320  3月16      ││
│            │  └─────────────────────────────────┘│
└────────────┴─────────────────────────────────────┘

● = 进行中    ○ = 已结账
```

## 技术栈

- **Python 3.10+**
- **Tkinter** - GUI界面框架
- **Pillow** - 图片处理库
- **JSON** - 数据存储格式

## 项目结构

```
construction-project-accounting/
├── main.py                     # 应用程序入口
├── start.bat                   # Windows一键启动脚本
├── config/                     # 配置文件目录
│   ├── app_config.json        # 应用配置
│   ├── user_config.json       # 用户配置
│   ├── requirements.txt       # 依赖包
│   └── default.json           # 默认配置
├── src/                        # 源代码目录
│   ├── gui/                   # 图形界面包
│   │   ├── __init__.py       # 包初始化
│   │   ├── main_window.py    # 主窗口组装
│   │   ├── sidebar.py        # 左侧项目列表
│   │   ├── content.py        # 右侧内容区
│   │   ├── theme.py          # 颜色/字体常量
│   │   ├── widgets.py        # 通用组件
│   │   └── dialogs/          # 弹窗组件
│   │       ├── new_project.py    # 新建项目
│   │       ├── edit_trade.py     # 编辑工作项目
│   │       └── edit_bill.py      # 编辑账单记录
│   ├── project_manager.py     # 项目管理模块
│   ├── calculator.py          # 计算器模块
│   ├── image_output.py        # 图片导出模块
│   ├── config_loader.py       # 配置加载模块
│   ├── logger.py              # 日志模块
│   └── utils.py               # 工具函数
├── projects/                   # 项目数据目录
├── backups/                    # 备份文件目录
├── logs/                       # 日志文件目录
└── export_previews/            # 导出预览图目录
```

## 安装与运行

### 环境要求
- Python 3.10 或更高版本
- pip 包管理器

### 安装步骤

1. 克隆或下载项目文件
2. 安装依赖包：
   ```bash
   pip install -r config/requirements.txt
   ```
3. 运行应用程序：
   ```bash
   python main.py
   ```

### 一键启动
Windows用户可以直接双击 `start.bat` 文件启动应用程序。

## 使用说明

### 创建新项目
1. 点击左侧边栏的「+ 新建项目」按钮
2. 输入项目名称
3. 点击「创建」按钮

### 切换项目
直接点击左侧边栏中的项目名称即可切换。

### 项目状态管理
- 项目默认状态为「进行中」
- 点击「标记为已结账」可将项目归档
- 已结账的项目显示为灰色

### 管理工作类型
1. 点击右侧的「工作类型」标签
2. 可以添加、编辑、删除工作类型
3. 支持恢复默认工作类型

### 记录账单
1. 点击右侧的「账单管理」标签
2. 点击「+ 添加记录」按钮
3. 选择工作项目
4. 输入公式（支持算式如 3×4+5）
5. 填写备注（可选）
6. 日期默认为当天，可修改
7. 点击「确定」保存

### 编辑/删除记录
- **编辑**：双击记录行
- **删除**：选中记录后点击「删除选中」

### 导出图片
1. 点击「保存为图片」按钮
2. 选择保存位置
3. 图片自动生成

## 术语说明

| 术语 | 含义 |
|------|------|
| 工作类型 | 工人的工种类别，如泥瓦工程、水电工程 |
| 工作项目 | 具体的工作内容，如砌墙、贴砖 |
| 按单价计费 | 需要"单价（浮点）"和"单位（文本）"，按 单价×公式结果 计算，如 45 元/㎡ × 5㎡ |
| 无单价计费 | 不需要单价和单位，金额直接由公式得出（按次/一次性） |
| 进行中 | 项目正在施工或未结清 |
| 已结账 | 钱已付清，项目关闭 |

## 数据结构

### 项目数据
```json
{
  "name": "XX小区装修",
  "status": "active",
  "created_at": "2024-03-15",
  "last_modified": "2024-03-15 10:30:00",
  "trade_items": [...],
  "bills": [...]
}
```

### 记录数据
```json
{
  "trade_item_name": "泥瓦工程 - 砌墙",
  "has_unit": true,
  "category": "泥瓦工程",
  "unit_price": 45,
  "unit": "元/平方米",
  "content": "5",
  "total": 225,
  "note": "加油",
  "work_date": "2024-03-15",
  "record_time": "2024-03-15 10:30:00"
}
```

## 配置说明

### 应用配置 (app_config.json)
- `button_font_size`: 按钮字体大小
- `operator_mapping`: 操作符映射关系（×、÷等符号转换）
- `default_trade_items`: 默认工作类型列表

### 用户配置 (user_config.json)
- `export_text_color`: 导出文字颜色
- `export_bg_color`: 导出背景颜色

### 环境变量
- `CPA_PROJECTS_DIR`: 项目数据目录（默认：./projects）
- `CPA_BACKUPS_DIR`: 备份文件目录（默认：./backups）
- `CPA_CONFIG_DIR`: 配置文件目录（默认：./config）
- `CPA_LOG_LEVEL`: 日志级别（DEBUG/INFO/WARNING/ERROR）

## 数据备份

系统自动在 `backups/` 目录中创建项目备份文件，备份文件按时间戳命名，保留最近10个备份。

## 日志文件

应用程序运行日志保存在 `logs/app.log` 文件中，包含调试信息和操作记录。

## 许可证

本项目为私人使用，未经授权禁止商业使用。